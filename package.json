{
  "name": "pure-css-slider",
  "version": "1.0.0",
  "description": "Pure CSS Slider",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url pure-css-slider",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "7.2.0",
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.6.1",
    "sass": "^1.29.0"
  },
  "keywords": [
    "css",
    "slider"
  ]
}
